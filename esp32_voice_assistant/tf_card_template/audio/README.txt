PCM 文件格式：16000Hz、16-bit、有符号小端、单声道、无 WAV 头。

wake.pcm                 唤醒提示音
shutdown.pcm             休眠提示音
commands/time.pcm        时间固定指令
commands/test.pcm        本地播放测试

可使用 tools/prepare_audio.py 将 PCM WAV 转换为裸 PCM。
